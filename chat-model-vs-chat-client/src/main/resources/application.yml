server:
  servlet:
    encoding:
      enabled: true
      force: true
      charset: UTF-8

spring:
  application:
    name: chat-model-vs-chat-client
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}