server:
  port: 8085
  servlet:
    context-path: /prompt
    encoding:
      enabled: true
      force: true
      charset: UTF-8

spring:
  application:
    name: prompt
  thymeleaf:
    prefix: classpath:/templates/
    cache: false
    encoding: UTF-8
    mode: HTML