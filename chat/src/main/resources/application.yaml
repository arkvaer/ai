server:
  port: 8899
  servlet:
    context-path: /api

spring:
  ai:
    dashscope:
      agent:
        api-key: ${AI_DASHSCOPE_API_KEY}
      api-key: ${AI_DASHSCOPE_API_KEY}
  application:
    name: chat